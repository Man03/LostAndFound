import React from "react";

export default function AdminDashboard() {

    const handleAddCoClick = async () => {
        
    }

  return (
    <div>
      <div>
        <button className="add-co" onClick={handleAddCoClick}>+ Add Coordinator</button>
      </div>
    </div>
  );
}
